def aumentar(n, p):
    aumento = n * (p * 0.01)
    finala = n + aumento
    return finala


def diminuir(n, p):
    desconto = n * (p * 0.01)
    finald = n - desconto
    return finald


def dobro(n):
    d = n * 2
    return d


def metade(n):         
    m = n / 2
    return m   
